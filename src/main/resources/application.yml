server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/point?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: yj_mysql

  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: /.jsp


mybatis:
  type-aliases-package: com.point.www
  mapper-locations: mybatis/mapper/**/*.xml
  configuration:


